# 국내·글로벌 결제 연결 준비

## 결정

- 국내 결제, 해외 발급 카드, 해외 간편결제를 **Toss Payments 서버 API**로 통합한다.
- PayPal Business Payment Link는 글로벌 지갑의 선택적 대체 경로로 유지한다.
- 정적 GitHub Pages에는 공개 라우팅 설정만 두고 시크릿 키, 결제 승인, 웹훅, 환불은 `pay.stargateedu.co.kr` 백엔드에서 처리한다.
- 현재 상태: 16개 SKU와 결제 선택 UI가 준비됨. `payment-links.js`의 `toss.apiBase`가 비어 있어 실결제는 안전하게 비활성화됨.

## 고객 결제 경로

1. 국내 결제: 한국 발급 카드, 계좌이체, 국내 간편결제, KRW.
2. 해외카드: Visa, Mastercard, JCB, UnionPay, AMEX, KRW.
3. 글로벌 지갑: PayPal, Alipay, GCash 등 계약된 수단, USD.

## 연결 절차

1. Toss Payments MID와 테스트 클라이언트 키·시크릿 키를 발급한다.
2. 해외카드와 `FOREIGN_EASY_PAY` 사용 가능 여부 및 업종 심사를 확인한다.
3. 별도 서버리스 프로젝트를 `pay.stargateedu.co.kr`에 배포한다.
4. 서버 환경변수에 키를 등록하고 주문 생성·승인·웹훅·취소 API를 구현한다.
5. `TOSS_BACKEND_SPEC.md`의 금액검증·멱등성·상태전이 조건을 검증한다.
6. 테스트 결제 성공·실패·사용자 취소·금액변조·중복승인·웹훅 재전송·부분환불을 확인한다.
7. `payment-links.js`의 `toss.apiBase`에 검증된 HTTPS 백엔드 주소를 입력한다.
8. 라이브 키로 교체한 뒤 소액 실결제와 전액취소를 수행한다.

## 출시 전 차단 항목

- 푸터의 사업자등록번호 `000-00-00000` 및 통신판매업 신고번호 `제0000호` 교체
- 이용약관의 “온라인 결제 기능은 추후 도입 예정” 문구 개정
- 개인정보처리방침에 Toss Payments 결제 위탁, 처리 항목, 해외지갑 사용 시 국외 이전 내용을 실제 계약에 맞춰 반영
- 디지털 콘텐츠 제공 개시와 청약철회 제한에 대한 별도 사전 동의
- USD 상품 가격을 서버 상품 테이블에 명시적으로 등록
- 결제 성공 후 LMS 수강권·eBook 만료 링크 지급 절차 확정

브라우저 성공 페이지 이동만으로 콘텐츠를 지급하지 않는다. 서버 저장 상태와 Toss 조회·웹훅 결과가 최종 결제 완료일 때만 지급한다.
